$min-width		=			400
$max-width		=			800
$min-width-px	=			unit($min-width, px)
$max-width-px	=			unit($max-width, px)
$min-font		=			16
$max-font		=			22
$min-font-px	=			unit($min-font, px)
$max-font-px	=			unit($max-font, px)

:root
	font-size   			unit($min-font, "px")

@media (min-width: $min-width-px) and (max-width: $max-width-px)

	:root
		font-size			"calc(%s + (%s - %s) * ((100vw - %s) / (%s - %s)))" % (($min-font-px) ($max-font) ($min-font) ($min-width-px) ($max-width) ($min-width))

@media (min-width: $max-width-px)

	:root
		font-size			unit($max-font, "px")

// Base styles

// @name Body text
// @description Plain text anywhere on the site.
// @markup
// <p>Prow scuttle parrel provost Sail ho shrouds spirits boom mizzenmast yardarm. Pinnace holystone mizzenmast quarter crow's nest nipperkin grog yardarm hempen halter furl. Swab barque interloper chantey doubloon starboard grog black jack gangway rutters.</p>

html,
body
	color					$body-text-colour
	font-family				$body-font-backup
	font-kerning			auto
	font-smooth				always
	font-weight				300
	letter-spacing			0.5px
	text-rendering			optimizeLegibility
	-webkit-font-smoothing	subpixel-antialiased

	&.wf-active
		font-family 		$body-font

		body
			font-family 	$body-font

p
	line-height				1.365

// @name Headers
// @description All kinds of header elements.
// @markup
// <h1>Header</h1>
// <h2>Header</h2>
// <h3>Header</h3>
// <h4>Header</h4>
// <h5>Header</h5>
// <h6>Header</h6>
// <h1>Header
//   <div class="u-subheader"> with subheader</div>
// </h1>

h1,
h2,
h3,
h4,
h5,
h6
	font-weight				100
	margin					0